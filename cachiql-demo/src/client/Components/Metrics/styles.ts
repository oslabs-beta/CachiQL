import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  depositContext: {
    flex: 1,
    paddingTop: '30px',
    paddingBottom: '30px',
  },
  sparkle: {
    position: 'relative',
  },
  trendIcon: {
    display: 'flex',
    alignItems: 'center',
    height: 50,
    width: 50,
  },
});